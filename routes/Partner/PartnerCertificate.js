const express = require('express');
const certificateRoute = express.Router();

PartnerRoute.get("/")
PartnerRoute.post("/")
PartnerRoute.get("/:certificateId")

PartnerRoute.put("/:certificateId")
PartnerRoute.delete("/:certificateId")

module.exports = certificateRoute;