const express = require('express');
const serviceRoute = express.Router();

PartnerRoute.put("/:servicesId")
PartnerRoute.delete("/:servicesId")
PartnerRoute.post("")
PartnerRoute.get("/")
PartnerRoute.get("/:servicesId")

module.exports = serviceRoute;